<%= form_for(@wishline) do |f| %>
  <% if @wishline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wishline.errors.count, "error") %> prohibited this wishline from being saved:</h2>

      <ul>
      <% @wishline.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :quantity %><br />
    <%= f.number_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
