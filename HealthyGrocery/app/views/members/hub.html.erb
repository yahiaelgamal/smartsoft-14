<!-- This the Users/Admins Hub Page which is the Home page that shows New/Recommended Items -->

<h1> Hub </h1>
<% @tmp = current_member %>
<t> User ID = <%= @tmp.id %> </t>
<t> User ID = <%= current_member.id %> </t>
<t> User ID = <%= current_member.id %> </t>
<opt><%= link_to 'Go Shopping',{:controller  => 'items', :action => 'members_items_index'}, :class => "opt"%></opt></br></br>

