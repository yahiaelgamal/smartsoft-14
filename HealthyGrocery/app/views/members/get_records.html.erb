<!-- Author: mina sedra -->
<!-- Team: 2-->
<!-- This page is the view of the  members'health record  -->
<!DOCTYPE html>
<br/>
<br/>

<head>

<style>
.form_caja {
        width: 524px;
        padding-top: 8px;
        padding-bottom: 15px;
        margin: 0 auto 20px auto;
        background-color: #25242c;    
         border-radius: 15px;    
        -moz-border-radius: 15px;    
        -webkit-border-radius: 15px;
        color: #446bb3;
        padding:10px;
         /*opacity:0.8;*/
    }
    
    
    body { background-color: #39b1c6;
}
    table { background-color : #25242c; color : #453}
    thead {  background-color: #32313b; color :#fff; padding:4px; line-height:30px;}
    </style>


</head>

<body>
<div class="contextual">
      <%= link_to(new_healthrecord_path, class: "btn btn-success") do %>
        New Healthrecord <i class="icon-plus icon-white"></i>
      <% end %>
    </div>
    <div class="form_caja">
  <p > <%= current_member.first_name %> 's HEALTHRECORDS</p>
  <table width="100%" >
  
<% @health_records.each do |healthrecord| %>
  <thead><tr> 
  
      <td>Name: <%= healthrecord.name %></td> 
<td><%= link_to  "<button>Show</button>".html_safe, healthrecord %></td>
    <td><%= link_to  "<button>Edit</button>".html_safe, edit_healthrecord_path(healthrecord) %></td>
    <td><%= link_to  "<button>Remove</button>".html_safe, healthrecord, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr></thead>
  <% end %>
  </table>
</div>

</body>

